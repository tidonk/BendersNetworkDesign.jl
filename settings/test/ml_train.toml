[MODEL]
model_type = "benders"

[SOLVER]
SOLVER = "Gurobi"
time_limit = 300

[SCENARIOS]
num_outage_scenarios = 15

[BENDERS.SUBPROBLEM_SCORING]
ordering = "score"

[BENDERS.SUBPROBLEM_SCORING.score]
weights = [0.05, 0.0, 0.8, 0.05, 0.1, 0.01]  # Small ML weight to enable training
scale_score = true

[BENDERS.SUBPROBLEM_SELECTION]
strategy = "static"
stabilization_frequency = 20
score_initialization_enabled = true

[BENDERS.SUBPROBLEM_SELECTION.static]
min_score_threshold = 0.5
iteration_time_limit = -1.0

[BENDERS.SUBPROBLEM_SELECTION.static.cut_limit]
mode = "absolute"
absolute = -1

[BENDERS.SUBPROBLEM_SELECTION.static.consecutive_miss]
mode = "absolute"
absolute = 100

[BENDERS.CUT_FILTERING]
strategy = "none"

[BENDERS.ML]
model_write = true
model_read = false

[LOGGING]
statistics = true
ml_statistics = true
print_solution = false
